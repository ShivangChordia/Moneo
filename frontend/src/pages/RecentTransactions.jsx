const RecentTransactions = () => {
    return (
        <div>
            <h2>Investment List</h2>
        </div>
    );
};

export default RecentTransactions; // Make sure this is included!
